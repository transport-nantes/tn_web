# Copy this file to settings_local.py, which must not be in git.
# In production, the settings_local.py is provided by saltstack.

import os

SECRET_KEY = 'SECRETt//FsXQCQfS3gGHCRRUlly3if6PxB6sAJuBeS2xlf2kALwpCtV7tnaD/1Ck='
DEBUG = True
ROLE = 'dev'

ALLOWED_HOSTS  = ['localhost', '127.0.0.1', '[::1]']
# ALLOWED_HOSTS  = ['beta*.transport-nantes.com', '127.0.0.1', '[::1]']
# ALLOWED_HOSTS  = ['www*.transport-nantes.com', '127.0.0.1', '[::1]']

MORE_INSTALLED_APPS = [
]

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
    }
}
